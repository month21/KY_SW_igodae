# 여러 약 인식 — "한 장씩 촬영 누적" 백업 방안

> **언제 쓰나:** YOLO(여러 약 한 번에 탐지)가 잘 안 될 경우의 **대체 방식**.
> SAM은 attractor 문제(어떤 이미지든 페라트라정/크레메진으로 샘)로 폐기. 단일약 인식은 강하니 **약을 한 알씩 단일 인식해서 리스트에 쌓는** 방식이 가장 안정적.

---

## 핵심 아이디어
여러 약을 한 사진에 우겨넣지 말고(=SAM 필요=깨짐), **약을 하나씩 촬영 → 각각 단일 인식(강함) → 결과 리스트에 누적** → 다 찍으면 종합(병용 경고 포함).

```
여러약 모드 진입
 → 카메라(줌으로 약 1개 크게)
 → [촬영] 약1 단일 인식 → "1개 인식됨" 리스트 추가
 → [촬영] 약2 단일 인식 → "2개 인식됨" 추가
 → ... 반복
 → [촬영 완료] 버튼
 → "약 N개 인식했습니다. 맞습니까?" 최종 확인 화면
 → 확인 → 전체 결과 + DUR 병용금기/상호작용 경고
```

---

## 구현 항목 (App.jsx)

### 1. 카메라 줌(확대) 기능 — `CameraView`
- 약 하나만 화면에 크게 잡도록 줌 필요
- 방법 A: `track.applyConstraints({ advanced: [{ zoom: value }] })` (지원 기기) + 줌 슬라이더
- 방법 B(폴백): CSS `transform: scale()` + 중앙 크롭 (디지털 줌)
- 줌 슬라이더 UI (1x ~ 3x)

### 2. 여러약 모드 = 촬영 누적
- 상태: `capturedPills` 배열 (각 약의 단일 인식 결과 누적)
- 촬영할 때마다 `fetchModelInference`(단일, SAM 안 씀)로 인식 → 결과를 `capturedPills`에 push
- 카메라 화면에 **"현재 N개 인식됨"** 뱃지 표시
- 같은 약 중복 촬영 시 dedup(이름 앞 4글자) 또는 사용자에게 "이미 인식된 약" 알림

### 3. "촬영 완료" 버튼
- 카메라 화면 하단에 [촬영 완료] 버튼
- 누르면 누적 촬영 종료 → 최종 확인 단계로

### 4. "약 N개 인식했습니다, 맞습니까?" 최종 확인
- 인식된 약 리스트(이름 + 썸네일/유사도) 보여주기
- 각 항목 **삭제 가능**(잘못 인식된 거 빼기) + [다시 촬영] 추가 가능
- [확인] → 전체 결과 화면 + DUR(병용금기·임부·노인·효능군중복) 경고

---

## 기존 코드 재사용 포인트
- **단일 인식**: `fetchModelInference()` (이미 강함, SAM 미사용)
- **약 정보 조회**: `analyzeSinglePill()` 그대로 (각 약마다 호출)
- **병용 경고**: `runDurCheck(results, userProfile)` — 누적된 전체 약 리스트로 1번 호출
- **종합 분석**: `analyzePillsCombined(results)` — 2개 이상일 때
- **결과 표시**: 기존 `pillResults` 다중 표시 로직 활용

## 주의
- 단일약 모드는 지금처럼 그대로 (사진 1장 = 약 1개 즉시 인식)
- 이 방식은 **여러약 모드**에만 적용
- DUR은 백그라운드(이미 적용) 유지
- 식약처 1장짜리 약(AIHub 없는 약)은 어차피 약함 — 데모는 AIHub 약 위주

---

## 장점 vs SAM/YOLO
| 방식 | 정확도 | 비고 |
|------|--------|------|
| SAM (현재 여러약 모드) | ❌ 깨짐 | attractor로 오인식, 폐기 |
| YOLO (학습 중) | ❓ 미검증 | 한 번에 여러 약, 되면 베스트 |
| **한 장씩 누적 (이 방안)** | ✅ 강함 | 단일 인식 그대로 쓰니 확실, 발표 안전 |

→ **YOLO 실패 시 이 방식으로 전환하면 여러 약도 확실하게 처리 가능.**
